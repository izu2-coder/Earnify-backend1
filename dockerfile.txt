# Use official lightweight Python image FROM python:3.11-slim # Set working directory WORKDIR /app # Copy requirements file first to leverage Docker layer caching COPY requirements.txt requirements.txt RUN pip install --no-cache-dir -r requirements.txt # Copy all app files COPY . . # Expose the port your Flask app runs on EXPOSE 5000 # Command to run the app CMD ["gunicorn", "-b", "0.0.0.0:5000", "app:app"]